services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-hashi}
      POSTGRES_USER: ${POSTGRES_USER:-hashi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-hashi}
    volumes:
      - hashi_pg_dev:/var/lib/postgresql/data

volumes:
  hashi_pg_dev:
